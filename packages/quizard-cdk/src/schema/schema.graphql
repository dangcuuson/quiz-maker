schema {
  query: Query
  mutation: Mutation
}

type Query {
  quizList(topic: String!): [Quiz!]!
  topicList: [String!]!
  testLambda(value: String!): String!
}

type Mutation {
  addQuiz(input: QuizInput!): Quiz!
}