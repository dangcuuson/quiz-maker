schema {
  query: Query
  mutation: Mutation
}

type Query {
  quizList(topic: String!): [Quiz!]!
  quizItem(id: ID!): Quiz
  topicList: [String!]!
}

type Mutation {
  addQuiz(input: QuizInput!): Quiz!
}