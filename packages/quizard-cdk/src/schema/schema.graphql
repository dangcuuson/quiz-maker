schema {
  query: Query
  mutation: Mutation
}

type Query {
  quizList(topic: String!): [Quiz!]!
  topicList: [String!]!
}

type Mutation {
  addQuiz(input: QuizInput!): Quiz!
  populateQuizData: Int!
}