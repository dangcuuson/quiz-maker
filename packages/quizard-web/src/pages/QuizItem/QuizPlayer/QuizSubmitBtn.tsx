import { Button } from '@aws-amplify/ui-react';
import React from 'react';

const QuizSubmitBtn: React.FC = () => {
    return <Button variation="primary">Submit</Button>;
};

export default QuizSubmitBtn;
